spring:
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 10MB
      # 设置总上传的文件大小
      max-request-size: 20MB
  # 数据源
  datasource:
    url: jdbc:mysql://localhost:3306/manager_system
    username: root
    password: "0427"
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

